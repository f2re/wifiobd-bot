# WifiOBD VK Bot

Профессиональный VK-бот для интернет-магазина [wifiobd.ru](https://wifiobd.ru) с полной интеграцией OpenCart и YooKassa.

## 🚀 Возможности

### Для пользователей
- 🛍 **Просмотр каталога** - навигация по категориям и товарам
- 🛒 **Корзина покупок** - управление товарами в корзине
- 💳 **Оплата через YooKassa** - безопасная оплата банковскими картами
- 📦 **История заказов** - просмотр своих заказов
- 💬 **Поддержка** - система обращений в службу поддержки
- ⭐ **VK-специфичные функции** - интеграция с VK Pay, геолокация, вложения

### Для администраторов
- 📋 **Управление заказами** - просмотр, обработка, отмена заказов
- 💬 **Обработка обращений** - ответы на вопросы пользователей  
- 👥 **Управление пользователями** - просмотр базы пользователей
- 📊 **Статистика** - аналитика продаж и активности
- 📢 **Рассылка** - массовые уведомления пользователям сообщества

## 🏗 Архитектура

### Технологический стек
- **Python 3.11+**
- **vkbottle 4.3+** - современный асинхронный фреймворк для VK Bot API
- **PostgreSQL** - основная база данных бота
- **Redis** - кэширование и хранение корзин
- **MySQL/MariaDB** - чтение данных из OpenCart (read-only)
- **YooKassa API** - платежная интеграция
- **Docker Compose** - контейнеризация

### Интеграция с OpenCart 3.0.2.0

Используется **гибридный подход**:
- **Чтение данных из БД** - категории, товары (быстрее и надежнее)
- **Запись через API** - создание заказов через OpenCart 3.0.2.0 API (безопаснее)

**Поддерживаемая версия OpenCart:** 3.0.2.0 (rs.4)
API использует аутентификацию через Username и Key (не токен!)

### Интеграция с VK

**VK Bot API возможности:**
- Inline-клавиатуры для навигации
- Callback-кнопки для быстрых действий
- VK Pay для альтернативных платежей (опционально)
- Геолокация для указания адреса доставки
- Вложения (фото товаров, документы)
- Карусели товаров

## 📦 Установка и настройка

### Требования
- **Docker** версии 20.10+ с встроенным Docker Compose V2
  - Используется команда `docker compose` (не `docker-compose`)
- VK сообщество (группа) с правами администратора
- Токен VK сообщества
- YooKassa аккаунт с Shop ID и Secret Key
- Доступ к базе данных OpenCart (read-only)

### Шаг 1: Создание VK сообщества

1. Создайте сообщество на [vk.com/groups](https://vk.com/groups)
2. Включите сообщения сообщества:
   - Управление → Сообщения → Включить сообщения
3. Настройте Long Poll API:
   - Управление → Работа с API → Long Poll API
   - Включите Long Poll API
   - Версия API: 5.131+
4. Получите токен доступа:
   - Управление → Работа с API → Ключи доступа
   - Создайте токен с правами:
     - Управление сообществом
     - Сообщения сообщества
   - Сохраните токен в `.env`

### Шаг 2: Клонирование репозитория

```bash
git clone <repository-url>
cd wifiobd-bot
git checkout vk-bot
```

### Шаг 3: Настройка окружения

Скопируйте файл с примером конфигурации:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и укажите свои данные:

```ini
# VK Bot
VK_TOKEN=vk1.a.your_community_token_here
VK_GROUP_ID=123456789
ADMIN_IDS=123456789,987654321

# OpenCart
OPENCART_URL=https://wifiobd.ru
OPENCART_DB_HOST=your_db_host
OPENCART_DB_USER=readonly_user
OPENCART_DB_PASSWORD=your_password

# YooKassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

### Шаг 4: Настройка OpenCart

#### 4.1 Создание Read-Only пользователя БД

Подключитесь к MySQL/MariaDB и выполните:

```sql
CREATE USER 'bot_readonly'@'%' IDENTIFIED BY 'secure_password';
GRANT SELECT ON opencart.* TO 'bot_readonly'@'%';
FLUSH PRIVILEGES;
```

#### 4.2 Настройка OpenCart API (для создания заказов)

В админ-панели OpenCart 3.0.2.0:
1. Перейдите в **System → Users → API**
2. Нажмите **Add New** (или редактируйте существующий API)
3. Заполните данные:
   - **Username**: например, "Default" или "VKBot"
   - **Status**: Enabled
   - Нажмите **Generate** для создания API Key
4. Скопируйте **Username** и **Key**
5. В `.env` файле укажите:
   ```ini
   OPENCART_API_USERNAME=Default
   OPENCART_API_KEY=ваш_сгенерированный_ключ
   ```

**Важно:** Без настройки API заказы не будут создаваться в OpenCart!

### Шаг 5: Настройка YooKassa

1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. Создайте магазин в личном кабинете
3. Получите Shop ID и Secret Key:
   - Настройки → Ключи API
4. Настройте уведомления:
   - Настройки → Уведомления
   - HTTP-уведомления: Включить
   - URL: `https://your-domain.ru/api/yookassa/webhook`
5. Укажите данные в `.env`

### Шаг 6: Настройка Callback API (для входящих сообщений)

1. В настройках сообщества VK:
   - Управление → Работа с API → Callback API
2. Укажите адрес сервера:
   - URL: `https://your-domain.ru/api/vk/callback`
3. Подтверждение сервера:
   - Скопируйте строку подтверждения
   - Добавьте в `.env`: `VK_CONFIRMATION_CODE=your_code`
4. Включите типы событий:
   - message_new
   - message_reply  
   - message_edit
   - group_join

### Шаг 7: Запуск бота

```bash
# Сборка и запуск контейнеров
docker compose up -d --build

# Просмотр логов
docker compose logs -f bot

# Остановка
docker compose down
```

### Шаг 8: Проверка работы

1. Перейдите в сообщения вашего сообщества VK
2. Отправьте команду "Начать"
3. Проверьте работу каталога и корзины
4. Протестируйте создание заказа

## 📁 Структура проекта

```
wifiobd-bot/
├── app/
│   ├── database/
│   │   ├── models.py              # Модели БД бота
│   │   └── opencart_models.py     # Модели OpenCart (read-only)
│   ├── handlers/
│   │   ├── start.py               # Главное меню
│   │   ├── catalog.py             # Каталог товаров
│   │   ├── cart.py                # Корзина
│   │   ├── checkout.py            # Оформление заказа
│   │   ├── payment.py             # Оплата
│   │   ├── admin.py               # Админ-панель
│   │   └── support.py             # Поддержка
│   ├── keyboards/
│   │   ├── inline.py              # VK Inline клавиатуры
│   │   └── carousel.py            # Карусели товаров
│   ├── middlewares/
│   │   ├── throttling.py          # Анти-спам
│   │   └── database.py            # БД сессии
│   ├── services/
│   │   ├── opencart.py            # Интеграция OpenCart
│   │   ├── cart.py                # Сервис корзины
│   │   ├── yookassa.py            # Платежи YooKassa
│   │   ├── vk_service.py          # VK API сервис
│   │   ├── user.py                # Пользователи
│   │   └── order.py               # Заказы
│   ├── states/
│   │   ├── checkout.py            # FSM для оформления
│   │   └── admin.py               # FSM для админки
│   ├── utils/
│   │   ├── logger.py              # Логирование
│   │   ├── formatting.py          # Форматирование текста
│   │   ├── pagination.py          # Пагинация
│   │   └── vk_helpers.py          # VK вспомогательные функции
│   ├── bot.py                     # Инициализация VK бота
│   └── main.py                    # Точка входа
├── config/
│   ├── settings.py                # Конфигурация
│   └── opencart_db.py             # OpenCart БД
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── .env.example
└── README.md
```

## 🎮 Использование

### Команды бота

**Для пользователей:**
- "Начать" - Главное меню
- "Каталог" - Просмотр товаров
- "Корзина" - Моя корзина
- "Заказы" - История заказов
- "Помощь" - Справка
- "Поддержка" - Связаться с поддержкой

**Для администраторов:**
- "Админ" - Админ-панель
- "Заказ #ID" - Просмотр заказа
- "Обращение #ID" - Просмотр обращения

### Админ-панель

Доступна только пользователям VK, указанным в `ADMIN_IDS`.

Функции:
- Просмотр и управление заказами
- Ответы на обращения в поддержку
- Просмотр пользователей и статистики
- Рассылка сообщений подписчикам сообщества

## 🔒 Безопасность

### Рекомендации

- ✅ Используйте **read-only** доступ к БД OpenCart
- ✅ Храните токены в `.env` файле (не коммитьте в Git!)
- ✅ Включите rate limiting (уже настроен)
- ✅ Настройте HTTPS для Callback API
- ✅ Проверяйте подписи VK запросов
- ✅ Регулярно обновляйте зависимости
- ✅ Делайте резервные копии БД

### Переменные окружения

Никогда не храните секретные данные в коде!
Все чувствительные данные должны быть в `.env`:

```bash
# Добавьте .env в .gitignore
echo ".env" >> .gitignore
```

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Только логи бота
docker compose logs -f bot

# Логи с фильтром
docker compose logs -f bot | grep ERROR
```

### Файлы логов

Логи сохраняются в директории `logs/`:
- `bot_YYYY-MM-DD.log` - общие логи
- `errors_YYYY-MM-DD.log` - только ошибки

## 🔧 Troubleshooting

### Бот не отвечает в VK

1. Проверьте, запущен ли контейнер:
   ```bash
   docker compose ps
   ```

2. Проверьте логи:
   ```bash
   docker compose logs -f bot
   ```

3. Проверьте настройки Callback API или Long Poll в VK

4. Убедитесь, что токен VK валиден и имеет нужные права

### Ошибки подключения к БД

1. Проверьте доступность OpenCart БД:
   ```bash
   docker compose exec bot ping OPENCART_DB_HOST
   ```

2. Проверьте данные для подключения в `.env`

3. Убедитесь, что создан read-only пользователь

### Проблемы с YooKassa

1. Проверьте Shop ID и Secret Key
2. Убедитесь, что магазин активен в YooKassa
3. Проверьте настройку webhook уведомлений
4. Проверьте логи на наличие ошибок API

### Ошибки VK API

1. **Error 5 (Authorization failed)**: Проверьте токен
2. **Error 100 (Invalid parameter)**: Проверьте параметры запроса
3. **Error 901 (Can't send messages to this user)**: Пользователь заблокировал сообщения

## 🔄 Обновление

```bash
# Остановить контейнеры
docker compose down

# Обновить код
git pull origin vk-bot

# Пересобрать и запустить
docker compose up -d --build
```

## 💻 Разработка

### Локальная разработка

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить бота локально
python -m app.main
```

### Добавление новых функций

1. Создайте новый handler в `app/handlers/`
2. Зарегистрируйте обработчик в `app/handlers/__init__.py`
3. Добавьте необходимые клавиатуры в `app/keyboards/`
4. При необходимости создайте новые states

## 🆚 Отличия от Telegram версии

### Преимущества VK версии:
- Интеграция с VK экосистемой
- Возможность использования VK Pay
- Геолокация и вложения
- Карусели товаров
- Встроенная аналитика VK

### Технические отличия:
- Использование vkbottle вместо aiogram
- Callback API или Long Poll вместо Telegram webhook
- Другой формат клавиатур и кнопок
- Особенности работы с вложениями

## 📝 Миграция с Telegram версии

Если вы используете Telegram версию бота и хотите добавить VK:

1. База данных полностью совместима
2. Все сервисы (OpenCart, YooKassa, Redis) используются без изменений
3. Можно запускать оба бота параллельно
4. Пользователи идентифицируются по VK ID вместо Telegram ID

## 📄 Лицензия

MIT

## 🤝 Поддержка

По вопросам и предложениям:
- Email: support@wifiobd.ru
- VK: [vk.com/wifiobd](https://vk.com/wifiobd)
- Telegram: [@wifiobd_bot](https://t.me/wifiobd_bot)

## 🙏 Благодарности

- [vkbottle](https://github.com/vkbottle/vkbottle) - мощный фреймворк для VK ботов
- [OpenCart](https://www.opencart.com/) - платформа для интернет-магазинов
- [YooKassa](https://yookassa.ru) - платежная система
- [VK API](https://dev.vk.com/) - социальная платформа
